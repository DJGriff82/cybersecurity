<h1>User Details</h1>

<p><strong>Name:</strong> <%= [@user.first_name, @user.last_name].compact.join(" ") %></p>
<p><strong>Email:</strong> <%= @user.email %></p>
<p><strong>Role:</strong> <%= @user.role.humanize %></p>
<p><strong>Company:</strong> <%= @user.company&.name || "—" %></p>
<p><strong>Status:</strong> <%= @user.deleted_at.present? ? "Disabled" : "Active" %></p>

<hr>

<%= link_to "Edit", edit_super_user_path(@user) %> |
<%= link_to "Back to Users", super_users_path %> |

<% if @user.deleted_at.present? %>
  <%= link_to "Enable", toggle_active_super_user_path(@user),
              method: :patch,
              data: { confirm: "Re-enable this user?" } %>
<% else %>
  <%= link_to "Disable", toggle_active_super_user_path(@user),
              method: :patch,
              data: { confirm: "Are you sure you want to disable this user?" } %>
<% end %>
